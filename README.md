# ASCII-Video
A simple, realtime video capture to ASCII Art converter that runs in the terminal.

## Dependencies
Python packages install command: 
```
pip install numpy opencv-python
```
## Usage
You can adjust the resolution and characters used by changing some variables at the top of the code.

When running if you get a ```can't open camera by index``` error, try changing the ```cam_index``` variable to another one digit number.

[Screencast_20251228_193547.webm](https://github.com/user-attachments/assets/abe6224e-8704-412b-a200-2871edbd08a9)
